# IT Request Analytics Dashboard

```bash
npm install
npm run dev
npm run build
```

The dashboard uses structured data generated by `npm run extract:data`. The source PDFs are not stored in `public/` and are excluded from deployment.
